package util.pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import util.testng.GalenBaseTest;

import java.net.MalformedURLException;

/**
 * Created by win on 1/28/16.
 */
public class NhLandingPage extends GalenBaseTest{

    public static void main(String[] args) {
        // TODO Auto-generated method stub
    }

    public void goToNewhomesLandingPage(String country) throws Exception {

        //getDriver().navigate().refresh();
        String url = "";

//        if ("MY".equalsIgnoreCase(country)) {
//            browser.get(URLStore.getHomePage() + "/" + URLStore.url_nh_landing_page_my);
//        }else if ("ID".equalsIgnoreCase(country)) {
//            browser.get(URLStore.getHomePage() + "/" + URLStore.url_nh_landing_page_id);
//        }else if ("DD".equalsIgnoreCase(country)) {
//            browser.get(URLStore.getHomePage() + "/" + URLStore.url_nh_landing_page_dd);
//        }else if ("DDEN".equalsIgnoreCase(country)) {
//            browser.get(URLStore.getHomePage() + "/" + URLStore.url_nh_landing_page_dden);
//        }else if ("SG".equalsIgnoreCase(country)) {
//            browser.get(URLStore.getHomePage() + "/" + URLStore.url_nh_landing_page_sg);
//        }


    }

}
